```git
git show HEAD
git reset HEAD <finename>
git reset <commit num>  // first 7 num is fine

git merge <branch_name>  // on other branch

git clone remote_location clone_name

git merge origin/master

git push origin <my branch>
```

